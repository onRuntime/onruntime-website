---
title: Użycie Next.js
description: Korzystanie z @onruntime/translations z Next.js App Router
---
## Ustawienia dla App Router

### 1. Skonfiguruj Dostawcę

Stwórz plik providers, aby ustawić tłumaczenia:

```tsx
// app/providers.tsx
"use client";

import { TranslationsProvider } from "@onruntime/translations/react";
import { ReactNode } from "react";

import en from "@/locales/en";
import fr from "@/locales/fr";

const locales = { en, fr };

interface ProvidersProps {
  children: ReactNode;
  locale: string;
}

export function Providers({ children, locale }: ProvidersProps) {
  return (
    <TranslationsProvider locale={locale} locales={locales}>
      {children}
    </TranslationsProvider>
  );
}
```

### 2. Ustaw Układ Główny

Użyj dostawcy w swoim układzie głównym:

```tsx
// app/[locale]/layout.tsx
import { Providers } from "../providers";

export default function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: { locale: string };
}) {
  return (
    <Providers locale={params.locale}>
      {children}
    </Providers>
  );
}
```

## Komponenty Serwera

Użyj `getTranslation` dla Komponentów Serwera:

```tsx
// app/[locale]/page.tsx
import { getTranslation } from "@/lib/translations.server";

export default async function HomePage() {
  const { t } = await getTranslation("common");

  return (
    <div>
      <h1>{t("title")}</h1>
      <p>{t("description")}</p>
    </div>
  );
}
```

<Callout type="warning" title="Serwer vs Klient">
  Użyj `getTranslation` w Komponentach Serwera i `useTranslation` w Komponentach Klienta.
</Callout>

## Komponenty Klienta

Użyj `useTranslation` dla Komponentów Klienta:

```tsx
"use client";

import { useTranslation } from "@onruntime/translations/react";

export function LanguageSwitcher() {
  const { locale, setLocale } = useTranslation("common");

  return (
    <select value={locale} onChange={(e) => setLocale(e.target.value)}>
      <option value="en">English</option>
      <option value="fr">Français</option>
    </select>
  );
}
```

## Routing oparty na lokalizacji

### Ustawienia Middleware

Skonfiguruj middleware do obsługi wykrywania lokalizacji:

```tsx
// middleware.ts
import { NextRequest, NextResponse } from "next/server";

const locales = ["en", "fr"];
const defaultLocale = "en";

export function middleware(request: NextRequest) {
  const pathname = request.nextUrl.pathname;

  // Sprawdź, czy ścieżka ma lokalizację
  const pathnameHasLocale = locales.some(
    (locale) => pathname.startsWith(`/${locale}/`) || pathname === `/${locale}`
  );

  if (pathnameHasLocale) return;

  // Przekieruj do domyślnej lokalizacji
  return NextResponse.redirect(
    new URL(`/${defaultLocale}${pathname}`, request.url)
  );
}

export const config = {
  matcher: ["/((?!api|_next|.*\\..*).*)"],
};
```

## Generacja statyczna

Generuj statyczne strony dla wszystkich lokalizacji:

```tsx
// app/[locale]/page.tsx
export async function generateStaticParams() {
  return [{ locale: "en" }, { locale: "fr" }];
}
```

## Komponent Link

Użyj przetłumaczonego komponentu Link do nawigacji:

```tsx
import { Link } from "@onruntime/translations/next";

function Navigation() {
  return (
    <nav>
      <Link href="/">Home</Link>
      <Link href="/about">About</Link>
    </nav>
  );
}
```

Komponent `Link` automatycznie obsługuje prefiksowanie lokalizacji.

## Przykład struktury projektu

<Files>
  <Folder name="app" defaultOpen>
    <Folder name="[locale]" defaultOpen>
      <File name="layout.tsx" />
      <File name="page.tsx" />
      <Folder name="about">
        <File name="page.tsx" />
      </Folder>
    </Folder>
    <File name="providers.tsx" />
  </Folder>
  <Folder name="locales" defaultOpen>
    <Folder name="en">
      <File name="common.json" />
    </Folder>
    <Folder name="fr">
      <File name="common.json" />
    </Folder>
  </Folder>
  <File name="middleware.ts" />
</Files>
