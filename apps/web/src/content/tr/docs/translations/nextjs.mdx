---
title: Next.js kullanımı
description: Next.js App Router ile @onruntime/translations kullanımı
---
## App Router için Kurulum

### 1. Sağlayıcıyı Yapılandır

Çevirileri ayarlamak için bir sağlayıcı dosyası oluştur:

```tsx
// app/providers.tsx
"use client";

import { TranslationsProvider } from "@onruntime/translations/react";
import { ReactNode } from "react";

import en from "@/locales/en";
import fr from "@/locales/fr";

const locales = { en, fr };

interface ProvidersProps {
  children: ReactNode;
  locale: string;
}

export function Providers({ children, locale }: ProvidersProps) {
  return (
    <TranslationsProvider locale={locale} locales={locales}>
      {children}
    </TranslationsProvider>
  );
}
```

### 2. Ana Düzeni Kur

Sağlayıcıyı ana düzeninizde kullanın:

```tsx
// app/[locale]/layout.tsx
import { Providers } from "../providers";

export default function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: { locale: string };
}) {
  return (
    <Providers locale={params.locale}>
      {children}
    </Providers>
  );
}
```

## Sunucu Bileşenleri

Sunucu Bileşenleri için `getTranslation` kullan:

```tsx
// app/[locale]/page.tsx
import { getTranslation } from "@/lib/translations.server";

export default async function HomePage() {
  const { t } = await getTranslation("common");

  return (
    <div>
      <h1>{t("title")}</h1>
      <p>{t("description")}</p>
    </div>
  );
}
```

<Callout type="warning" title="Sunucu vs İstemci">
  Sunucu Bileşenlerinde `getTranslation` ve İstemci Bileşenlerinde `useTranslation` kullan.
</Callout>

## İstemci Bileşenleri

İstemci Bileşenleri için `useTranslation` kullan:

```tsx
"use client";

import { useTranslation } from "@onruntime/translations/react";

export function LanguageSwitcher() {
  const { locale, setLocale } = useTranslation("common");

  return (
    <select value={locale} onChange={(e) => setLocale(e.target.value)}>
      <option value="en">İngilizce</option>
      <option value="fr">Fransızca</option>
    </select>
  );
}
```

## Yerel Tabanlı Yönlendirme

### Ara Yazılım Kurulumu

Yerel tespitini yönetmek için ara yazılımı ayarlayın:

```tsx
// middleware.ts
import { NextRequest, NextResponse } from "next/server";

const locales = ["en", "fr"];
const defaultLocale = "en";

export function middleware(request: NextRequest) {
  const pathname = request.nextUrl.pathname;

  // Pathname'in bir yerel içerip içermediğini kontrol et
  const pathnameHasLocale = locales.some(
    (locale) => pathname.startsWith(`/${locale}/`) || pathname === `/${locale}`
  );

  if (pathnameHasLocale) return;

  // Varsayılan yerel için yönlendir
  return NextResponse.redirect(
    new URL(`/${defaultLocale}${pathname}`, request.url)
  );
}

export const config = {
  matcher: ["/((?!api|_next|.*\\..*).*)"],
};
```

## Statik Üretim

Tüm yerellere statik sayfalar oluştur:

```tsx
// app/[locale]/page.tsx
export async function generateStaticParams() {
  return [{ locale: "en" }, { locale: "fr" }];
}
```

## Bağlantı Bileşeni

Navigasyon için çevrili Bağlantı bileşenini kullan:

```tsx
import { Link } from "@onruntime/translations/next";

function Navigation() {
  return (
    <nav>
      <Link href="/">Anasayfa</Link>
      <Link href="/about">Hakkında</Link>
    </nav>
  );
}
```

`Link` bileşeni otomatik olarak yerel ön eklemeleri yönetir.

## Örnek Proje Yapısı

<Files>
  <Folder name="app" defaultOpen>
    <Folder name="[locale]" defaultOpen>
      <File name="layout.tsx" />
      <File name="page.tsx" />
      <Folder name="about">
        <File name="page.tsx" />
      </Folder>
    </Folder>
    <File name="providers.tsx" />
  </Folder>
  <Folder name="locales" defaultOpen>
    <Folder name="en">
      <File name="common.json" />
    </Folder>
    <Folder name="fr">
      <File name="common.json" />
    </Folder>
  </Folder>
  <File name="middleware.ts" />
</Files>
